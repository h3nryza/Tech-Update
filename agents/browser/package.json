{
  "name": "tech-update-browser-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test --reporter=json > ../reports/browser-report.json 2>&1; npx playwright test --reporter=list",
    "test:headed": "npx playwright test --headed",
    "install-browsers": "npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@axe-core/playwright": "^4.11.2",
    "serve": "^14.2.0"
  }
}
